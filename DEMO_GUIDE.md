# คู่มือนำเสนอ — ข้อ 2 "Showcase Application (base on GitHub) → Demo"

## คำถามหลัก: รันบน localhost แล้วนำเสนอได้ไหม

**ได้ครับ และเป็นวิธีที่ควรใช้**

"base on GitHub" ในโจทย์ **ไม่ได้แปลว่าต้องเอาเว็บขึ้นอินเทอร์เน็ต** แต่แปลว่า
*สิ่งที่คุณเดโมต้องเป็นโค้ดชุดเดียวกับที่อยู่บน GitHub* — อาจารย์เปิด repo ตามได้
โหลดไปรันเองก็ได้ผลเหมือนกัน ไม่ใช่เดโมจากโค้ดในเครื่องที่ไม่มีใครตรวจสอบได้

ระบบนี้เป็น **แอปที่มีฐานข้อมูลจริงและ 4 container** การรันบนเครื่องตัวเองด้วย Docker
คือวิธีที่นิ่งที่สุด และตรงกับที่โจทย์ให้ใช้ Docker Compose อยู่แล้ว

> **ข้อได้เปรียบที่เพิ่งได้มา** — รอบที่ผ่านมาเราตัด CDN ภายนอกออกหมดแล้ว
> เว็บนี้จึงทำงานได้เต็มรูปแบบ **แม้เน็ตห้องเรียนล่ม** ซึ่งเป็นเหตุผลที่ดีมาก
> ที่จะเดโมจากเครื่องตัวเองแทนการพึ่งเว็บออนไลน์

---

## ซ้อมวันนี้: พิสูจน์ว่า "โค้ดบน GitHub รันได้จริง"

นี่คือการซ้อมที่คุ้มที่สุด เพราะมันตอบโจทย์ "base on GitHub" ตรง ๆ
และจับได้ทันทีถ้ามีไฟล์สำคัญตกหล่นไม่ได้ถูก push ขึ้นไป

### ขั้นที่ 1 — ปิดของเดิมก่อน (สำคัญมาก)

```powershell
cd "C:\Users\Prame\Claude\Projects\WORK WEBSITE UNIVERSITY\bookvice"
docker compose down
```

> **ห้ามข้ามขั้นนี้** — ชื่อ container ถูกกำหนดตายตัวว่า `bookvice-api` `bookvice-db` …
> และพอร์ต 3000 · 8000 · 5433 · 8080 ก็ใช้ตัวเดียวกัน
> ถ้าไม่ปิดของเดิม ตัวใหม่จะสร้างไม่ขึ้นเพราะชื่อและพอร์ตชนกัน
>
> **`down` เฉย ๆ ไม่ลบข้อมูล** ห้ามใส่ `-v` เด็ดขาด ไม่งั้นฐานข้อมูลหาย

### ขั้นที่ 2 — โคลนจาก GitHub มาใหม่หมด

```powershell
cd "$env:USERPROFILE\Desktop"
git clone https://github.com/67160333/WebApplicationDevelopment.git bookvice-check
cd bookvice-check
Copy-Item .env.example .env
docker compose up -d --build
```

### ขั้นที่ 3 — ดูว่าขึ้นครบไหม

```powershell
docker compose logs api --tail 20
```

ต้องเห็น

```
เชื่อมต่อฐานข้อมูลสำเร็จ
กำลังใส่ข้อมูลตัวอย่าง...
Bookvice API พร้อมใช้งานที่ http://localhost:8000/docs
```

แล้วเปิด `http://localhost:3000` ล็อกอิน `mind` / `Password123` ลองจองคิวหนึ่งครั้ง

**ถ้าผ่านหมด แปลว่าโค้ดบน GitHub สมบูรณ์** ใครโหลดไปก็รันได้ ไม่ต้องพึ่งไฟล์ในเครื่องคุณ

### ขั้นที่ 4 — เก็บกวาด กลับมาใช้ของเดิม

```powershell
cd "$env:USERPROFILE\Desktop\bookvice-check"
docker compose down -v
cd "C:\Users\Prame\Claude\Projects\WORK WEBSITE UNIVERSITY\bookvice"
docker compose up -d
```

โฟลเดอร์ `bookvice-check` บนเดสก์ท็อปลบทิ้งได้เลย

> ตรงนี้ใส่ `-v` ได้ เพราะเป็นฐานข้อมูลชั่วคราวของตัวทดสอบ
> **แต่ต้องแน่ใจว่าอยู่ในโฟลเดอร์ `bookvice-check` จริง ๆ** ดูที่ prompt ก่อนกด

---

## เช็กลิสต์วันนำเสนอ

**ก่อนถึงคิว 15 นาที**

- [ ] เปิด Docker Desktop รอจนไอคอนเขียว
- [ ] `cd` เข้าโฟลเดอร์ `bookvice` แล้ว `docker compose up -d`
- [ ] `docker compose ps` ต้องขึ้นครบ 4 ตัว
- [ ] เปิด `http://localhost:3000` กด `Ctrl+Shift+R` หนึ่งครั้ง แล้ว**ทิ้งแท็บไว้**
- [ ] เปิดแท็บที่สอง `http://localhost:8000/docs`
- [ ] เปิดแท็บที่สาม หน้า GitHub repo
- [ ] ล็อกอินค้างไว้เป็น `spaowner` ในหน้าต่างหนึ่ง (จะได้ไม่ต้องพิมพ์รหัสตอนเดโม)

**ห้ามทำเด็ดขาด**

- ❌ อย่าสั่ง `docker compose down -v` ในโฟลเดอร์ `bookvice` — ข้อมูลหายหมด
- ❌ อย่า build ใหม่ตอนใกล้ถึงคิว — ถ้าพังจะไม่มีเวลาแก้
- ❌ อย่าพึ่ง Wi-Fi ห้องเรียน (แต่ถึงเน็ตล่มเว็บก็ยังทำงานได้ เพราะไม่มี CDN แล้ว)

---

## ลำดับ Demo ที่แนะนำ (ประมาณ 3 นาที)

เริ่มจาก **GitHub ก่อน** เพราะโจทย์เขียนว่า base on GitHub

1. **เปิดหน้า repo** — ชี้ให้เห็น `app/` `web/` `docker-compose.yml`
   แล้วเลื่อนลงมาที่ตาราง **"ตรวจตามใบงาน — ครบทั้ง 10 เส้นทาง"** ใน README
   *"เส้นทางที่โจทย์ยกตัวอย่างมา อยู่ครบในตารางนี้ครับ"*

2. **สลับไป Swagger** `localhost:8000/docs` — เลื่อนให้เห็นหมวด
   **"9. เส้นทางตามรูปแบบในโจทย์"** แล้วกดยิง `POST /login` ให้ดูสักเส้น
   *"ทั้งระบบมี 56 เส้นทาง เกินที่โจทย์ขอ เพราะทำเป็นระบบจองคิวที่ใช้ได้จริง"*

3. **สลับไปหน้าเว็บ** `localhost:3000` — จองคิวให้ดูตั้งแต่ต้นจนจบ
   เลือกร้าน → เลือกบริการ → เลือกวัน → เลือกเวลา → ยืนยัน
   *"ช่องเวลาที่เห็นคือคิวว่างจริง คำนวณจากตารางงานของช่างกับคิวที่ถูกจองไปแล้ว"*

4. **สลับเป็นเจ้าของร้าน** เปิดหน้าจัดการร้าน ให้เห็นคิวที่เพิ่งจองเข้ามาโผล่ในระบบ
   *"อีกฝั่งหนึ่งเห็นทันที ไม่ต้องรอใครมาบอก"*

**จุดขายที่ควรพูดถ้ามีเวลาเหลือ** — กลับไปที่หน้าจอง แล้วชี้ว่าช่องที่เพิ่งจองไป
กลายเป็นสีจางกดไม่ได้แล้ว *"ระบบกันจองซ้ำให้ตั้งแต่หน้าจอ ไม่ใช่ไปเจอตอนกดยืนยัน"*

---

## ถ้าอยากได้ลิงก์สาธารณะด้วย (ไม่บังคับ)

**อัปเดต 20 ส.ค. 2026 — Hugging Face ไม่ฟรีแล้วสำหรับงานแบบนี้**
เอกสารทางการระบุว่า Docker Space ต้องสมัคร PRO ก่อน ส่วนที่ฟรีมีแค่ Static Space
ซึ่งรันฐานข้อมูลไม่ได้ โปรเจกต์นี้จึงใช้ทางนั้นแบบฟรีไม่ได้อีก

ทางเลือกที่ยังฟรีอยู่คือ **Render.com** (รองรับ Docker · 512 MB RAM · หลับเมื่อไม่มีคนใช้
· ต้องผูกบัตรตอนสมัครแม้ไม่ถูกเรียกเก็บ) ใช้คู่กับฐานข้อมูลฟรีจาก Neon
ไฟล์ `Dockerfile.hf` และโฟลเดอร์ `hf-upload` ที่เตรียมไว้ใช้ต่อได้ทั้งชุด

**ประเมินตามตรง**

| | รันในเครื่อง | ขึ้น Render |
|---|---|---|
| เวลาที่ต้องใช้ | 0 (พร้อมแล้ว) | ราว 1-2 ชั่วโมงถ้าไม่ติดอะไร |
| ต้องผูกบัตรเครดิตไหม | ไม่ต้อง | ต้อง (Render ขอตอนสมัคร) |
| ความเสี่ยงวันนำเสนอ | ต่ำ ควบคุมได้เอง | ขึ้นกับเน็ตห้องเรียนและเซิร์ฟเวอร์ฟรี |
| ตอบโจทย์ "base on GitHub" | ✅ ครบแล้ว | ✅ เท่ากัน |

**คำแนะนำ: เดโมในเครื่องก่อน** ถ้าเหลือเวลาว่างค่อยทำลิงก์สาธารณะเป็นของแถม
แล้วพูดตอนท้ายว่า *"ระบบพร้อมนำขึ้นเซิร์ฟเวอร์จริง มีไฟล์ตั้งค่าเตรียมไว้แล้วครับ"*
ซึ่งก็เป็นคะแนนอยู่ดีโดยไม่ต้องเสี่ยง

---

## แผนสำรอง ถ้าวันนำเสนอ Docker ไม่ขึ้น

1. **อัดวิดีโอเดโมไว้ล่วงหน้า** — กดอัดหน้าจอตอนซ้อม เก็บไฟล์ไว้ในเครื่องกับใน Google Drive
   ถ้าหน้างานพัง เปิดวิดีโอแทนได้ทันที ดีกว่ายืนงมต่อหน้าห้อง
2. **แคปหน้าจอสำคัญไว้** — หน้าแรก · หน้าจองคิว · หน้าจัดการร้าน · Swagger
3. ถ้าทุกอย่างล่ม ยังเปิด **หน้า GitHub** ให้ดูโค้ดได้เสมอ ซึ่งก็คือหัวใจของข้อ 2 อยู่แล้ว
