# Bookvice — บันทึกสถานะโปรเจกต์

> เอกสารนี้เขียนไว้ให้ผู้ช่วยคนใหม่อ่านแล้วทำงานต่อได้ทันที
> อัปเดตล่าสุด: 22 สิงหาคม 2026

---

## 1. โปรเจกต์นี้คืออะไร

**Bookvice** — สโลแกน *"จองง่าย ได้ครบ จบที่เดียว"*
แพลตฟอร์มจองคิวบริการ ทำเป็นงานส่งรายวิชา **89033167 Web Application Development**
อาจารย์ผู้สอน: JAKAPONG BOONYAI · ผู้จัดทำ: Prame

โจทย์กำหนดให้ทำ **REST API + Docker & Docker Compose**

เดิมทำเฉพาะบริการความงาม (สปา ทำเล็บ ทำผม คลินิก สักลาย)
ตอนนี้ขยายเป็น 8 หมวด เพิ่ม **สนามฟุตบอล · สนามแบดมินตัน · ส่งของด่วน**

> **เคยชื่อ GlowGo** — เปลี่ยนเป็น Bookvice เมื่อ 19 ส.ค. 2026
> เก็บชื่อเดิมครบทุกจุดแล้วเมื่อ 19 ส.ค. รวมถึงชื่อฐานข้อมูล ชื่อ container และเอกสารทุกไฟล์
>
> เหลือคำว่า `glowgo` อยู่ **3 ที่โดยตั้งใจ** ห้ามลบ:
> `web/js/api.js` (ย้ายคีย์ localStorage เดิม) · `app/migrate.py` (แก้ข้อมูลเดิมในฐานข้อมูล) ·
> ชื่อโฟลเดอร์โปรเจกต์ (เปลี่ยนได้ ดูท้ายหัวข้อ 2)

---

## 2. สั่งให้ระบบทำงาน

```powershell
cd "C:\Users\Prame\Claude\Projects\WORK WEBSITE UNIVERSITY\bookvice"
docker compose up -d --build
```

| ที่อยู่ | คืออะไร |
|---|---|
| http://localhost:3000 | หน้าเว็บ (nginx) |
| http://localhost:8000/docs | เอกสาร API (Swagger) |
| http://localhost:8080 | pgAdmin — ล็อกอินตามค่าใน `.env` |
| localhost:5433 | PostgreSQL (ต่อจากโปรแกรมภายนอก) |

### กฎสำคัญเรื่องการรีบิลด์

| แก้อะไร | ต้องทำอะไร |
|---|---|
| ไฟล์ใน `web/` | **แค่ `Ctrl+Shift+R`** — โฟลเดอร์นี้ bind-mount เข้า nginx |
| ไฟล์ใน `app/` | **ต้อง `docker compose up -d --build api`** — `app/` ถูก COPY เข้า image |
| `requirements.txt` · `docker-compose.yml` | ต้อง build ใหม่ |

> **จุดที่พลาดบ่อย:** `docker compose up -d --build api` สตาร์ตแค่ `api` กับ `db`
> ตัว `web` ไม่ขึ้นด้วย (เพราะ `web` เป็นฝ่ายพึ่ง `api` ไม่ใช่ทางกลับกัน)
> ถ้าพอร์ต 3000 ไม่ตอบ ให้รัน `docker compose up -d` เฉย ๆ ไม่ต้องระบุ service

### ชื่อโฟลเดอร์ต้องเป็น `bookvice`

ชื่อโฟลเดอร์กลายเป็นชื่อ project ของ Docker Compose ซึ่งกลายเป็นชื่อนำหน้า volume/image/network อีกที
**ถ้าย้ายเครื่องหรือเปลี่ยนชื่อโฟลเดอร์ Compose จะมองว่าเป็นคนละโปรเจกต์ แล้วสร้าง volume ใหม่ทั้งชุด**
ฐานข้อมูลสร้างเองอัตโนมัติจาก seed ภายในไม่กี่วินาที **แต่รูปที่เคยอัปโหลดเองจะค้างอยู่ใน volume เก่า**

> `container_name` ทั้ง 4 ตัวถูกกำหนดไว้ตรง ๆ ใน `docker-compose.yml` ว่า `bookvice-*` อยู่แล้ว
> ชื่อ container จึงไม่เปลี่ยนตามชื่อโฟลเดอร์ มีแต่ image/volume/network ที่เปลี่ยน

### บัญชีทดสอบ — รหัสผ่าน `Password123` ทุกคน

| username | บทบาท | ใช้ทดสอบอะไร |
|---|---|---|
| `mind` | customer | ฝั่งลูกค้า |
| `nong` | customer | ทดสอบการจองชนกันระหว่างสองคน |
| `spaowner` | owner | เจ้าของร้านความงาม |
| `nailowner` | owner | ทดสอบสิทธิ์ข้ามร้าน (ต้องถูกปฏิเสธ) |
| `venueowner` | owner | เจ้าของสนามกีฬาและศูนย์ส่งของ |
| `menowner` | owner | เจ้าของร้านหมวดกลุ่มผู้ชาย 4 หมวด |
| `admin` | admin | ผู้ดูแลระบบ |

---

## 3. สถาปัตยกรรม

```
docker-compose.yml
├── db        PostgreSQL 16-alpine   → volume db_data
├── api       FastAPI + uvicorn      → volume uploads_data (/app/uploads)
├── web       nginx:alpine           → bind-mount ./web
└── pgadmin   dpage/pgadmin4
```

**Backend:** FastAPI · Pydantic v2 · SQLAlchemy 2.0 (`Mapped`/`mapped_column`) · PyJWT · bcrypt · Pillow
**Frontend:** HTML + `web/css/app.css` (design system เขียนเอง รวมรีเซ็ตและยูทิลิตี้ครบในไฟล์เดียว) + vanilla JS — **ไม่พึ่ง CDN ภายนอกแล้ว**
**แผนที่:** Leaflet + OpenStreetMap โหลดจาก CDN เฉพาะหน้าที่ใช้จริง

### แบรนด์ ฟอนต์ และไอคอน

| | รายละเอียด |
|---|---|
| **ฟอนต์หัวข้อ** | **FC Mittraphap** (Fontcraft Studio) — ไฟล์อยู่ที่ `web/fonts/FCMittraphap-Heavy.ttf` |
| **ฟอนต์เนื้อความ** | IBM Plex Sans Thai จาก Google Fonts |
| **ไอคอน** | วาดเองใน `web/js/ui.js` วัตถุ `ICON` — ไม่มีลิขสิทธิ์ ไม่ต้องใส่เครดิต |
| **โลโก้** | ฟังก์ชัน `brandMark(size, spine)` ใน `web/js/ui.js` |

**เรื่องฟอนต์ที่ต้องรู้ — ชุดที่แจกฟรีมีน้ำหนักเดียว และเป็น Heavy (`usWeightClass = 850`)**

จึงใช้เป็น **ฟอนต์หัวข้ออย่างเดียว** ถ้าเอามาทำเนื้อความทั้งหน้า ตัวอักษรจะหนาเตอะ
จนอ่านยากและไม่เหลือลำดับความสำคัญให้เล่น เนื้อความจึงใช้ IBM Plex Sans Thai
ซึ่งมีน้ำหนักครบ 400–700 — นี่คือการจับคู่ "ฟอนต์หัวข้อหนา + ฟอนต์เนื้อความอ่านง่าย"
ซึ่งเป็นวิธีมาตรฐานของงานออกแบบ ไม่ใช่การแก้ขัด

**สัญญาอนุญาต:** ฟรีสำหรับงานที่ไม่ใช่เชิงพาณิชย์ (โปรเจกต์รายวิชาเข้าข่าย)
ถ้าใช้เชิงพาณิชย์ต้องสนับสนุน 500 บาท ที่ fontcraftstudio.com/support
สำเนาอยู่ที่ `web/fonts/LICENSE-FCMittraphap.txt` — **อย่าลบ**

> ไฟล์เป็น `.ttf` 104 KB ยังไม่ได้แปลงเป็น `.woff2` (จะเหลือราว 35 KB)
> เพราะเครื่องที่แปลงไม่มีตัวบีบอัด brotli — แปลงเพิ่มได้ที่ cloudconvert.com

**โลโก้:** ตัว B ประกอบจากห่วงสองวง ห่วงบนเป็นปฏิทิน ห่วงล่างเป็นเครื่องหมายถูก
`brandMark()` รับพารามิเตอร์ที่สองเป็น**สีแกนตั้ง** — พื้นเข้มต้องส่ง `"#fff"`
ไม่งั้นแกนสีกรมท่าจะกลืนหายไปกับแถบเมนู เหลือแค่ห่วงลอย ๆ (เคยพลาดมาแล้ว)

**สีจากโลโก้** เพิ่มใน `app.css`: `--coral-400/500` · `--teal-400/500`
ใช้เป็นสีเสริมเท่านั้น สีหลักของหน้าเว็บยังเป็นน้ำเงินกรมท่าซึ่งตรงกับสีเข้มในโลโก้อยู่แล้ว

### ตารางในฐานข้อมูล (12 ตาราง)

```
users · token_blacklist · categories · shops · shop_images · services
staff · notifications · shop_closures · bookings · reviews · payments
```

### ไฟล์ Python

| ไฟล์ | หน้าที่ |
|---|---|
| `app/main.py` | ประกอบแอป · lifespan รันลำดับ: รอ db → create_all → migrate → seed |
| `app/models.py` | ตารางทั้งหมด |
| `app/schemas.py` | Pydantic — ตรวจข้อมูลเข้า/ออก + สร้างเอกสาร Swagger |
| `app/migrate.py` | เติมคอลัมน์/เงื่อนไข/ดัชนีที่ขาด **โดยไม่ลบข้อมูลเดิม** รันซ้ำได้ |
| `app/security.py` | JWT · bcrypt · `require_roles()` |
| `app/storage.py` | จัดการไฟล์รูปที่อัปโหลด (ตรวจ · ย่อ · แปลง WebP) |
| `app/seed.py` | ข้อมูลตั้งต้น 3 ร้าน |
| `app/seed_extra.py` | เติมร้านความงาม + รีวิว + `backfill_coordinates()` |
| `app/seed_venues.py` | **สนามกีฬาและศูนย์ส่งของ** — ชื่อสมมติ แต่พิกัด/ราคา/เวลาอ้างอิงสถานที่จริง |
| `app/seed_men.py` | **4 หมวดที่เจาะกลุ่มผู้ชาย** — คลินิกสุขภาพชาย · ตัดผมนอกสถานที่ · ดูแลรถยนต์ · คาราโอเกะ |

### Router — 55 endpoint

| ไฟล์ | จำนวน | เรื่อง |
|---|---|---|
| `auth.py` | 4 | สมัคร · เข้าสู่ระบบ · ออก · เปลี่ยนรหัส |
| `users.py` | 6 | โปรไฟล์ · จัดการผู้ใช้ (admin) |
| `shops.py` | 17 | ร้าน · บริการ · ช่าง · วันหยุด · **ค้นหาตามระยะทาง** |
| `staff.py` | 2 | โปรไฟล์ช่างและรีวิวของช่าง |
| `bookings.py` | 13 | ตารางเวลาว่าง · จอง · เลื่อนนัด · รีวิว · **จองทันที** |
| `notifications.py` | 4 | กระดิ่งแจ้งเตือน |
| `images.py` | 4 | อัปโหลด/ลบ/ตั้งรูปปก |
| `payments.py` | 5 | มัดจำ · คืนเงิน · ใบเสร็จ · รายรับ |

---

## 4. แนวคิดหลักที่ต้องเข้าใจก่อนแก้โค้ด

### 4.1 ระบบคำนวณช่องเวลาว่าง — หัวใจของทั้งระบบ

อยู่ใน `app/routers/bookings.py` ฟังก์ชันสำคัญ:

| ฟังก์ชัน | ทำอะไร |
|---|---|
| `_windows_for(shop, member)` | คืน **รายการช่วงเวลาเปิดทำการ** ของหนึ่งวัน |
| `_fits_window(เวลา, นาที, ช่วง)` | คิวนี้เริ่มและจบในช่วงเดียวกันหรือไม่ |
| `_busy_intervals(...)` | ช่วงที่ถูกจองไปแล้ว หน่วยนาทีจากเที่ยงคืน |
| `_slot_is_taken(...)` | นับคิวที่คาบเกี่ยว เทียบกับความจุ |
| `_shop_capacity(db, shop_id, on_date)` | **จำนวนช่างที่เข้างานวันนั้นจริง** |
| `_staff_works_on(member, date)` | ช่างคนนี้เข้างานวันนั้นไหม |

**กติกาเรื่องเวลาเปิด-ปิด** (สำคัญมาก — เพิ่งแก้บั๊กใหญ่ตรงนี้):

| `open_time` vs `close_time` | หมายความว่า | `_windows_for` คืน |
|---|---|---|
| `10:00` < `20:00` | เวลาปกติ | `[(600, 1200)]` |
| `00:00` == `00:00` | **เปิด 24 ชั่วโมง** | `[(0, 1440)]` |
| `09:00` > `02:00` | **คร่อมเที่ยงคืน** | `[(0, 120), (540, 1440)]` |

> **ข้อจำกัดที่ตั้งใจออกแบบ:** หนึ่งการจองต้องจบภายในวันปฏิทินเดียวกัน
> จอง 23:30 ยาว 1 ชม. (จบ 00:30 ของวันถัดไป) จะถูกปฏิเสธ
> เลือกแบบนี้เพราะถ้าให้คร่อมวันได้ `booking_date` จะกำกวมทันที
> ว่า "01:00 ของวันที่ D" คือรอบดึกของ D-1 หรือรอบเช้าของ D

### 4.2 ทรัพยากรที่จองได้ — ใช้ตาราง `staff` ตัวเดียวกันหมด

**สนามบอล คอร์ทแบด และพนักงานส่ง เก็บในตาราง `staff` เหมือนช่างทำผม**
ต่างกันแค่ **คำเรียก** ซึ่งเก็บที่ `categories.resource_label`

| หมวด | `resource_label` | ชนิดทรัพยากร |
|---|---|---|
| spa-massage / nail / hair | ช่าง | คน |
| beauty-clinic | แพทย์/ผู้ดูแล | คน |
| tattoo | ช่างสัก | คน |
| **mens-clinic** | **แพทย์** | คน |
| **mobile-barber** | **ช่าง** | คน |
| football | สนาม | พื้นที่ |
| badminton | คอร์ท | พื้นที่ |
| **car-care** | **ช่องบริการ** | พื้นที่ |
| **karaoke** | **ห้อง** | พื้นที่ |
| delivery | พนักงานส่ง | ยานพาหนะ |

> **หน้าเว็บแยก "คน" กับ "สิ่งของ" ด้วยรายชื่อคำ ไม่ใช่ด้วยฟิลด์ในฐานข้อมูล**
> ตัวแปร `PERSON_LABELS` ใน `shop.html` และอาเรย์เดียวกันใน `bookings.html` · `manage.html`
> **เพิ่ม `resource_label` ใหม่ที่เป็นคน ต้องไปเพิ่มในทั้งสามที่** ไม่งั้นจะได้คำว่า
> "แพทย์ไหนก็ได้" แทนที่จะเป็น "ใครก็ได้"

Backend ส่งค่านี้มากับ `ShopOut.resource_label` และ `ShopDetail.resource_label`
หน้าเว็บ `shop.html` เก็บไว้ในตัวแปร `RES` แล้วใช้แทนคำว่า "ช่าง" ทุกที่ที่ผู้ใช้เห็น

> **ถ้าจะเพิ่มหมวดใหม่:** เพิ่มใน `seed_venues.py` → `NEW_CATEGORIES`
> และเพิ่มไอคอนใน `web/js/ui.js` → `ICON` + `CATEGORY_ICON`

### 4.3 การจองสองแบบ

`services.booking_mode` กำหนดว่าบริการนี้จองอย่างไร

| ค่า | เส้นทาง | ลักษณะ |
|---|---|---|
| `scheduled` (ค่าเริ่มต้น) | `POST /api/bookings` | เลือกวัน-เวลาจากปฏิทิน |
| `instant` | `POST /api/bookings/instant` | **ไม่มีปฏิทิน** กรอกต้นทาง-ปลายทาง กดเรียกเลย |

บริการ `instant` (ส่งของด่วน):
- ใช้เวลาปัจจุบันเป็น `booking_date` / `booking_time`
- ตั้ง `status = "confirmed"` ทันที ไม่ต้องรอร้านกดรับ
- คิดเงิน `price + price_per_km × distance_km`
- เก็บ `pickup_address` · `dropoff_address` · `distance_km`
- **ไม่ตรวจช่องเวลา** แต่ยังตรวจว่าร้านเปิดอยู่ ณ ขณะนั้น

ทั้งสองเส้นทางกันไขว้กัน — เรียกผิดเส้นทางได้ **400** พร้อมบอกว่าให้ไปใช้อันไหน

### 4.4 ระบบชำระเงิน — เป็นการจำลอง

ตาราง `payments` แยกออกมา เพราะการจอง 1 ครั้งจ่ายได้หลายรอบ (มัดจำ + ยอดคงเหลือ)

- `kind`: `deposit` (20%) / `balance`
- `method`: `promptpay` / `card` / `cash` — **`cash` เฉพาะเจ้าของร้าน**
- จ่ายมัดจำแล้ว **คิวเปลี่ยนจาก pending เป็น confirmed อัตโนมัติ**
- เลขที่ใบเสร็จ `RC-YYMM-00042` สร้างจาก `payment.id` หลัง flush
- ฟังก์ชัน `_mock_charge()` คือจุดเดียวที่ต้องแก้ถ้าจะต่อ payment gateway จริง

### 4.5 ระบบรูปภาพ — ความปลอดภัย 4 ชั้น

`app/storage.py` — **อย่าลดชั้นไหนออก** แต่ละชั้นกันคนละอย่าง

1. ตรวจ **ไบต์จริงต้นไฟล์** ไม่เชื่อ `Content-Type` ที่ส่งมา
2. **ตั้งชื่อไฟล์ใหม่แบบสุ่ม** ทิ้งชื่อเดิมทั้งหมด (กัน `../../etc/passwd`)
3. **เข้ารหัสรูปใหม่เป็น WebP เสมอ** — สคริปต์ที่แนบท้ายไฟล์รูปหายไปพร้อม EXIF (ซึ่งมีพิกัด GPS ผู้ถ่าย)
4. จำกัดขนาด 5 MB และจำกัดจำนวนพิกเซล (กัน decompression bomb)

รูปเก็บที่ `/app/uploads/shops/{shop_id}/{ชื่อสุ่ม}.webp` ผูกกับ volume `uploads_data`

### 4.6 แผนที่และระยะทาง

- `shops.latitude` / `shops.longitude` — `NUMERIC(9,6)`
- `GET /api/shops?near_lat=&near_lng=&radius_km=` — เรียงตามระยะทาง
- คำนวณ **haversine ในฐานข้อมูล** ไม่ใช่ในโค้ด Python
- **ต้อง clamp ค่าก่อนเข้า `acos`** ด้วย `least(1, greatest(-1, ...))` ไม่งั้นทศนิยมคลาดเคลื่อนจะทำให้ `acos(1.0000000002)` พัง
- หน้าเว็บ **ตัดพิกัดผู้ใช้ออกจาก URL เสมอ** — ตำแหน่งที่อยู่ไม่ควรค้างในประวัติเบราว์เซอร์

---

## 5. บั๊กที่แก้ไปแล้ว — อย่าทำพลาดซ้ำ

| # | อาการ | สาเหตุ | แก้อย่างไร |
|---|---|---|---|
| 1 | สนามเปิดถึงตี 2 หรือ 24 ชม. **จองไม่ได้เลยทั้งวัน** | `_window_for` คืน `(360, 120)` จุดเริ่ม > จุดจบ ลูป `while start + duration <= close` ไม่ทำงานสักรอบ | เปลี่ยนเป็น `_windows_for()` คืนรายการช่วง |
| 2 | คิวที่จบเที่ยงคืน **ไม่กันคิวถัดไป** | `_add_minutes` บีบชั่วโมงไว้ที่ 23 → 23:00+60 ได้ 23:00 → คิวยาว 0 นาที | ใช้ `% 1440` |
| 3 | ตัวกรอง "ว่างวันนี้" ตัดสนาม 24 ชม. ทิ้ง | `_has_free_slot` ใน `shops.py` ใช้โค้ดคนละชุดกับหน้าจอง | ให้เรียก `_windows_for` ตัวเดียวกัน |
| 4 | **รับคิวเกินจำนวนคนที่เข้างานจริง** | `_shop_capacity` นับช่างทุกคนที่ `is_active` ไม่ดูว่าวันนั้นเข้างานไหม | กรองด้วย `_staff_works_on()` ก่อนนับ · ส่ง `on_date` เข้าไปทั้ง 4 จุดที่เรียก |
| 11 | **ลบร้านที่มีคิวไม่ได้** — คำขอค้างจนการเชื่อมต่อหลุด | `Booking.service_id` ไม่มี `ondelete` · SQLAlchemy สั่ง `UPDATE bookings SET service_id=NULL` ทั้งที่คอลัมน์ห้ามว่าง | FK เพิ่ม `ondelete="CASCADE"` · relationship เพิ่ม `passive_deletes=True` · migrate เพิ่มชุด `RECREATE` แก้ FK เดิม |
| 5 | เลื่อนนัดแล้วนับคิวขาด | `filter()` ตัดทุกช่วงที่เวลาตรงกัน ไม่ใช่ตัดแค่ของตัวเอง | **วิธีแก้เดิม (`busy.remove(mine)`) ผิด** — ดูบั๊ก #14 ตอนนี้ใช้ `exclude_booking_id` กรองที่ SQL แทน |
| 6 | ไม่ส่ง token ได้ 403 แทน 401 | `HTTPBearer` ค่าเริ่มต้น `auto_error=True` | ตั้ง `auto_error=False` แล้วโยน 401 เอง |
| 7 | ลูกค้าแอบยัด `guest_phone` ได้ | เช็คสิทธิ์ดูแค่ `guest_name` | เช็คทั้งสองฟิลด์ |
| 8 | คะแนน 4.5 แสดง 4 ดาว | `Math.round(4.45) = 4` | วาดครึ่งดาวด้วย `linearGradient` |
| 9 | `<details>` พับแล้วเนื้อหายังโผล่ | คลาส `.grid` ของ Tailwind ชนะกฎซ่อนของเบราว์เซอร์ | เพิ่ม `details:not([open]) > *:not(summary) { display: none !important }` |
| 10 | เนื้อหาค้างมองไม่เห็นตอนเลื่อนเร็ว | `IntersectionObserver` ไม่ยิงถ้ากระโดดข้าม viewport | เปลี่ยนเป็นวัดตำแหน่งเองแบบ rAF-throttled |

### รอบตรวจบั๊กละเอียด 19 ส.ค. 2026 (รอบที่ 2)

ผู้ใช้สั่งว่า *"เช็คบัคทั้งหมดตรวจสอบอย่างละเอียดอย่าให้มีอะไรผิดพลาด"* — นี่คือสิ่งที่เจอ

**ฝั่งเซิร์ฟเวอร์**

| # | อาการ | สาเหตุ | แก้อย่างไร |
|---|---|---|---|
| 12 | คิวช่วงดึก **จองทับกันได้** ทั้งที่ควรชน | `_add_minutes` ใช้ `% 1440` (วิธีแก้บั๊ก #2) ทำให้ 23:00+90 ได้ 00:30 ซึ่ง "น้อยกว่า" เวลาเริ่ม การเทียบช่วงจึงไม่เจอการทับ | เพิ่ม `_span_minutes()` คืนค่านาทีดิบ **ห้าม mod** แล้วให้ทุกจุดที่ตรวจการชนใช้ตัวนี้ |
| 13 | ความจุร้านผิดเมื่อ **ไม่มีช่างเลย** | `_shop_capacity` คืน 0 ทั้งกรณี "ร้านไม่มีช่างในระบบ" และ "ไม่มีใครเข้างานวันนั้น" | แยกสองกรณี — ไม่มีช่างเลยคืน 1 (ร้านรับเอง) · มีช่างแต่ไม่เข้างานคืน 0 (ปิดรับ) |
| 14 | **เลื่อนนัดแล้วคิวคนอื่นหาย** | `busy.remove(mine)` ตัดช่วงเวลาที่ "หน้าตาเหมือนกัน" ตัวแรกที่เจอ ซึ่งอาจเป็นของลูกค้าคนอื่น | เพิ่ม `exclude_booking_id` ใน `_busy_intervals` กรองด้วย `Booking.id != ...` ที่ระดับ SQL |
| 15 | จองแยกกันสองทางแล้ว **เกินความจุ** | หน้าจอง (`get_availability`) กับตอนบันทึกจริง ใช้เกณฑ์คนละชุด | รวมเป็น `_assert_free()` ตัวเดียว เรียกทั้งตอนจองและตอนเลื่อนนัด |
| 16 | รหัสคิวชนกันได้ | เดิมสุ่มจาก 900 ค่า — วันที่มีคิวเกิน ~35 รายการมีโอกาสซ้ำเกิน 50% | ใช้ `secrets.token_hex(3)` = 16.7 ล้านค่า + ดัก `IntegrityError` |
| 17 | เวลาเพี้ยนไป 7 ชั่วโมง | `datetime.now()` ใน container เป็น UTC แต่ `Date`/`Time` ในฐานข้อมูลเป็นเวลาไทย | เพิ่ม `now_local()` ผูกกับ `Asia/Bangkok` แล้วตัด tzinfo ออก |
| 18 | ช่างที่เข้ากะข้ามคืน (22:00–02:00) **ไม่มีช่องว่างเลย** | `_windows_for` ตัดกะช่างแบบช่วงเดียว `lo < hi` เสมอ | ถ้า `lo > hi` แตกเป็นสองช่วง `[0,hi]` และ `[lo,DAY]` แล้วค่อยตัดกับเวลาเปิดร้าน |
| 19 | คิวที่ยกเลิกแล้ว **ปลุกกลับเป็นยืนยันได้ทับคิวใหม่** | `update_booking_status` ไม่ตรวจซ้ำตอนเปลี่ยนสถานะกลับ | เรียก `_assert_free()` ก่อนปลุกทุกครั้ง |
| 20 | **ผู้ใช้ทั่วไปดูโปรไฟล์คนอื่นได้** (IDOR) | `GET /api/users/{id}` เช็คแค่ว่าล็อกอินอยู่ | ต้องเป็นเจ้าของ id นั้นหรือเป็น admin เท่านั้น |
| 21 | token รูปแบบผิดทำเซิร์ฟเวอร์ **500** | อ่าน `payload["sub"]` ตรง ๆ | ครอบ try/except (KeyError/TypeError/ValueError) แล้วคืน 401 |
| 22 | รายงานยอดขาย **หักเงินคืนสองรอบ** | `received` หักคืนไปแล้ว แล้ว `net` หักซ้ำอีก | `received` = เงินที่รับมาทั้งหมด · `net` = หลังหักคืน หนึ่งครั้ง |
| 23 | สถานะการจ่ายผิดหลังคืนเงินบางส่วน | ดูจาก "มีใบเสร็จอะไรบ้าง" ไม่ได้ดูจากยอดเงิน | ตัดสินจากยอด: `paid >= total` = จ่ายครบ · `paid > 0` = จ่ายมัดจำ |
| 24 | `available_on` ยิง SQL ทีละร้าน (N+1) | ไม่ได้โหลด services มาพร้อมกัน | `selectinload(Shop.services)` |

**ฝั่งหน้าเว็บ**

| # | อาการ | สาเหตุ | แก้อย่างไร |
|---|---|---|---|
| 25 | **แท็บ "บริการของร้าน" ว่างทั้งแท็บ** — บั๊กหนักสุดของรอบนี้ | มีปุ่มพิมพ์สลิปหลงไปอยู่ใน `list.map()` ของ `renderServices()` แล้วอ้างตัวแปร `b` ที่ไม่มีในขอบเขตนั้น → ReferenceError หยุดสคริปต์ทั้งหน้า | ย้ายปุ่มไปไว้ใน `bookingRow()` ที่ควรอยู่ · เปลี่ยน `document.querySelectorAll("[data-pr]")` เป็น `panel.querySelectorAll(...)` |
| 26 | กดตอบกลับรีวิวครั้งเดียว เปิดกล่องซ้อนหลายอัน | `bindReplyButtons()` ผูก event ใหม่ทุกครั้งที่โหลดรีวิวหน้าถัดไป โดยไม่ถอดของเก่า | ใช้ event delegation ตัวเดียวผูกที่ `document` (ผูกที่ `#reviewList` ไม่ได้ เพราะยังไม่มีตอนสคริปต์รัน) |
| 27 | โหลดรีวิวหน้าถัดไปพลาด แล้ว **รีวิวที่มีอยู่หายหมด** | จับ error แล้วเขียนทับทั้งกล่อง | เก็บของเดิมไว้ แล้วต่อท้ายด้วยปุ่ม "ลองใหม่" |
| 28 | ค้นหาไม่เจอ ตัวเลขค้างที่ "กำลังค้นหา..." | เส้นทางผลลัพธ์ว่างไม่ได้เขียน `count.textContent` | เขียนข้อความ "ไม่พบร้าน..." ในเส้นทางนั้นด้วย |
| 29 | ชื่อทรัพยากรจากฐานข้อมูลไม่ได้ escape | `resource_label` ถูกยัดเข้า innerHTML ตรง ๆ | แยก `RES_RAW()` (ข้อความดิบ) กับ `RES() = esc(RES_RAW())` (ใส่ใน HTML) |
| 30 | สลิปพิมพ์คำว่า "ช่าง" ทุกร้านแม้เป็นสนาม | hard-code ไว้ใน `printSlip` | ส่ง `staffLabel` เข้าไป แล้วใช้ค่าจริงของหมวดนั้น |
| 31 | กระดิ่ง / เอฟเฟกต์เลื่อน / แผนที่ **ผูก event ทับซ้อนไปเรื่อย ๆ** | ผูกใหม่ทุกครั้งที่เรียก โดยไม่ถอดของเก่า | เก็บตัวถอดไว้ในตัวแปรระดับโมดูล (`_bellOutsideClick` · `_revealStop` · `pinMap`) แล้วถอดก่อนผูกใหม่ทุกครั้ง |
| 32 | ข้อมูลผู้ใช้ใน localStorage เสีย → **หน้าขาวทั้งหน้า** | `requireLogin()` เช็คแค่ token | เช็ค `this.user` ด้วย ถ้าไม่มีให้ `clear()` แล้วส่งไปหน้าล็อกอิน |

**ความลื่นไหล (ที่ผู้ใช้บอกว่า "ยังดูไม่สมูท")**

วัดด้วย Playwright + `PerformanceObserver({type:'layout-shift'})` พบว่าสาเหตุคือ
`#navbar` `#main` `#footer` เริ่มต้นสูง 0 แล้วให้ JavaScript เติมทีหลัง หน้าจึงกระโดดทุกครั้งที่ข้อมูลมาถึง

แก้โดยจองพื้นที่ไว้ล่วงหน้าใน `app.css` (`#navbar { min-height: 111px }` · `#footer { min-height: 620px }` ·
`#main { min-height: 68vh }`) และเปลี่ยนหน้ารอโหลดของ `shop.html` จากวงกลมหมุน
เป็นโครงร่างเทา ๆ (`.sk-page`) ที่มีสัดส่วนใกล้เคียงของจริง

| หน้า | CLS เดิม | CLS หลังแก้ |
|---|---|---|
| `index.html` | 0.1209 | **0.0136** |
| `shops.html` | 0.2876 | **0.0298** |
| `shop.html` | 0.6992 | **0** |

> เกณฑ์ของ Core Web Vitals: ต่ำกว่า 0.1 = ดี · 0.1–0.25 = ควรแก้ · เกิน 0.25 = แย่

### รอบเพดานการจอง + การชำระเงิน 21 ส.ค. 2026 (รอบที่ 3)

ผู้ใช้เจอเองตอนเข้าไปทดสอบ: *"สามารถจองล่วงหน้าได้ 20000 วันซึ่งมันไม่ควร"*

| # | อาการ | สาเหตุ | แก้อย่างไร |
|---|---|---|---|
| 33 | **จองล่วงหน้าได้ 20,000 วัน** (ถึงปี 2081) | ไม่มีใครตรวจขอบบนของ `booking_date` เลย — ตรวจแค่ว่าไม่ย้อนหลัง | เพิ่ม `MAX_ADVANCE_DAYS = 90` กับ `_assert_within_window()` แล้วเรียกทั้งตอนจอง ตอนเลื่อนนัด และตอนดูคิวว่าง |
| 34 | เลี่ยงเพดานได้ด้วยการ **จองพรุ่งนี้แล้วเลื่อนไปปี 2080** | `reschedule_booking` ไม่ได้ตรวจเหมือนตอนจอง | เรียก `_assert_within_window()` ก่อน `_closure_reason` |
| 35 | ช่องเลือกวันที่ในหน้าเลื่อนนัดและหน้าลงคิวหน้าร้าน **ไม่มี `max`** | ตั้งแต่ `min` อย่างเดียว ผู้ใช้พิมพ์ปีเองได้ | เพิ่ม `maxBookingDate()` ใน `api.js` แล้วเซ็ต `.max` ให้ `mvDate` และ `wiDate` |
| 36 | คืนเงินคิวที่ยัง **ใช้งานอยู่** ได้ | `refund_payment` ไม่ดูสถานะคิว | ต้องเป็น `cancelled` หรือ `completed` เท่านั้น ไม่งั้นคิวจะค้างในตารางทั้งที่คืนเงินไปแล้ว แล้วระบบจะกลับไปทวงมัดจำใหม่ |
| 37 | ลูกค้า **จ่ายมัดจำคิวที่เลยเวลาไปแล้ว** ได้ | ไม่ได้เทียบเวลานัดกับเวลาปัจจุบัน | บล็อกเฉพาะฝั่งลูกค้า (`not is_owner`) — ฝั่งร้านต้องกดรับเงินสดหลังลูกค้าใช้บริการเสร็จได้ตามปกติ |
| 38 | `_local_now()` ใน `payments.py` อ้าง `SHOP_TZ` ที่ไม่มีในไฟล์ | เขียนฟังก์ชันโดยไม่ได้นำเข้า | นำเข้า `now_local` จาก `bookings.py` แบบ lazy import ในฟังก์ชัน — ใช้นิยามเขตเวลาชุดเดียวกัน ไม่มีทางตั้งค่าเพี้ยนกัน |

**กฎที่ตั้งไว้จากรอบนี้**

- เพดานวันจองเป็นตัวเลขเดียวที่ต้องตรงกันสองฝั่ง — `MAX_ADVANCE_DAYS` ใน
  `app/routers/bookings.py` และใน `web/js/api.js` **แก้ตัวหนึ่งต้องแก้อีกตัว**
- ฝั่งหน้าเว็บมี `max` ไว้กันการกดผิด ไม่ใช่ไว้ตรวจแทนเซิร์ฟเวอร์
  เซิร์ฟเวอร์ยังตรวจซ้ำเสมอ เพราะช่อง `<input>` แก้ค่าจาก devtools ได้
- ปุ่มชำระเงินในหน้าเว็บต้องเขียนข้อความตามสิ่งที่เซิร์ฟเวอร์ยอมรับจริง
  รวมไว้ในฟังก์ชัน `payPlan()` ที่เดียว (`bookings.html`) ห้ามกระจายเงื่อนไขไปหลายที่

### รอบกติกาการล็อกคิวและค่าปรับยกเลิก 21 ส.ค. 2026 (รอบที่ 4)

โจทย์จากผู้ใช้: *"ลูกค้าต้องโดนหักค่ามัดจำ 20% ถ้ายกเลิก และต้องจ่ายแล้วเท่านั้น
ตารางเวลาที่เลือกถึงจะปิด ถ้ายังไม่จ่ายคนอื่นก็มาเลือกเวลาที่เราเลือกได้"*

**กติกาใหม่สองข้อ — จำให้ขึ้นใจก่อนแก้โค้ดส่วนจอง**

1. **ช่องเวลาจะปิดเมื่อ `holds_slot = TRUE` เท่านั้น** ไม่ใช่ปิดเพราะมีแถวในตาราง
   `bookings` คิวที่กดจองไว้แต่ยังไม่จ่ายจะไม่กันเวลาให้ใคร ใครจ่ายก่อนได้ก่อน
2. **ลูกค้ายกเลิกเอง = ไม่ได้ค่ามัดจำคืน** (มัดจำ = 20% ของค่าบริการ)
   ร้านยกเลิกเอง = คืนเต็มจำนวน · คิวที่ยังไม่จ่าย = ยกเลิกฟรี

`holds_slot` ตั้งเป็น TRUE เมื่อ

| เหตุการณ์ | ทำไม |
|---|---|
| มีการชำระเงินเข้ามา | หัวใจของกติกา — จ่ายแล้วได้สิทธิ์ |
| ร้านกดจองแทนลูกค้าหน้าร้าน (`guest_name`) | ร้านเป็นเจ้าของตาราง และรู้ว่าลูกค้ามาจริง |
| งานส่งของด่วน (instant) | พนักงานออกรถไปแล้วจริง |
| ร้านกดยืนยันคิวเอง | ร้านรับปากลูกค้าแล้ว ต้องกันเวลาให้แม้ยังไม่ได้เงิน |

| # | อาการ / สิ่งที่ทำ | รายละเอียด |
|---|---|---|
| 39 | คิวที่ยังไม่จ่าย **ล็อกตารางร้านได้ฟรี** | `_busy_intervals` นับทุกคิวที่สถานะ pending/confirmed เพิ่ม `Booking.holds_slot.is_(True)` เข้าไป |
| 40 | unique index เดิมกันคิวซ้ำเวลาแม้ยังไม่จ่าย | เปลี่ยนเป็น `uq_booking_held_slot` เงื่อนไข `WHERE holds_slot AND status IN (...)` · ตัวเก่าเข้า `DROP_INDEXES` |
| 41 | จ่ายเงินไปแล้วแต่ **คิวใช้ไม่ได้** เพราะโดนตัดหน้า | `pay_booking` เรียก `_assert_free` ก่อนตัดเงิน + ครอบ `db.commit()` ด้วย `IntegrityError` เป็นด่านสุดท้าย |
| 42 | ยกเลิกแล้วเงินมัดจำหายไปเงียบ ๆ | เพิ่ม `cancelled_by` และ `cancellation_fee` เก็บไว้ในฐานข้อมูล แสดงทั้งฝั่งลูกค้าและฝั่งร้าน |
| 43 | ยกเลิกได้สองทาง แต่แจ้งเตือนไม่เหมือนกัน | รวมเป็น `_apply_cancellation()` + `_notify_cancellation()` ใช้ร่วมกันทั้ง `DELETE /bookings/{id}` และ `PATCH .../status` |
| 44 | คอลัมน์ใหม่ทำให้คิวเก่ากลายเป็น "ไม่ล็อกเวลา" ทั้งหมด | migration เติมย้อนหลังให้คิวที่ยืนยันแล้วหรือมีเงินเข้ามาแล้ว |
| 45 | ชุดทดสอบบน SQLite เชื่อถือไม่ได้ | `postgresql_where` ใช้ได้เฉพาะ PostgreSQL — SQLite จะได้ unique index แบบไม่มีเงื่อนไข ต้องใส่ `sqlite_where` คู่กันเสมอ |
| 46 | SQLite ไม่บอกชื่อ index ใน error → ได้ 500 แทน 409 | `_slot_conflict` ดูรายชื่อคอลัมน์เป็นทางสำรอง |

| 47 | **จ่ายเงินเสร็จแล้วหน้าเว็บว่างเปล่า** — ผู้ใช้รายงานจากเว็บจริง | รายการถูกวาดใหม่หลังจ่ายเงิน การ์ดชุดใหม่เริ่มที่ `opacity: 0` อีกรอบ ตอนนั้นผู้ใช้เลื่อนอยู่กลางหน้า การ์ดที่ต่ำกว่าขอบจอจึงถูกซ่อนทั้งแถบ (ทำซ้ำได้: หลังกดจ่าย ซ่อน 8 จาก 10 ใบ) | `revealOnScroll` เล่นเอฟเฟกต์ **ครั้งเดียวต่อหนึ่งตัวเลือก** รอบวาดใหม่แสดงทันที + ถ้าเลื่อนไม่ได้แล้วให้แสดงที่เหลือทั้งหมด |

> **กฎ:** เอฟเฟกต์ค่อย ๆ ปรากฏมีไว้สำหรับ "การเห็นหน้านี้ครั้งแรก" เท่านั้น
> ห้ามให้มันทำงานตอนวาดข้อมูลใหม่หลังผู้ใช้กดอะไรสักอย่าง เพราะผู้ใช้จ้องผลลัพธ์อยู่
> และอาจเลื่อนหน้าค้างไว้ตรงไหนก็ได้

| 48 | **หน้าต่างพิมพ์ทำหน้าหลักค้างทั้งหน้า** | `printReceipt` เรียก `w.print()` จากหน้าหลัก ซึ่งเป็นคำสั่งหยุดรอ และหน้าต่างลูกใช้เธรดร่วมกับหน้าหลัก ผลคือเลื่อน/กดอะไรไม่ได้จนกว่าจะปิดกล่องพิมพ์ | ฝังสคริปต์ให้หน้าต่างลูกสั่ง `window.print()` เอง (`openPrintWindow`) และเลิกเด้งใบเสร็จอัตโนมัติหลังจ่ายเงิน |
| 49 | การ์ดผู้ให้บริการในแถวเดียวกัน **สูงไม่เท่ากัน** | `.staff-sched` ใช้ `space-between` + `flex-wrap` ความยาวข้อความจึงเป็นตัวตัดสินว่าเวลาอยู่บรรทัดไหน — "ตามเวลาร้าน" อยู่ข้างแถบวัน แต่ "09:00–18:00 น." ตกบรรทัด | บังคับ `flex-direction: column` สองบรรทัดตายตัว + `margin-top: auto` ดันเส้นคั่นกับปุ่มไปชิดขอบล่างทุกใบ |
| 50 | ส่วนท้ายเว็บ **จองพื้นที่ผิดบนมือถือ** | `#footer { min-height: 372px }` ค่าเดียว แต่ของจริงสูง 496px (แท็บเล็ต) และ 833px (มือถือ) · ซ้ำ `<footer>` ข้างในมี `margin-top: 80px` ที่ทะลุออกนอกกล่อง (margin collapsing) จึงไม่ถูกนับ | แบ่งเป็น 6 ช่วงตามที่วัดจริง + `display: flow-root` ขัง margin ไว้ข้างใน |
| 51 | หน้ารายการร้าน **กระโดด 0.13** | โครงร่างตอนรอโหลดวาดไว้ 6 ใบ แต่ API ส่งมา 9 ใบ (limit=9) · และ `#main { min-height: 68vh }` ไม่พอ ส่วนท้ายจึงโผล่มา 177px ในเฟรมแรกแล้วหายไป | โครงร่างเป็น 9 ใบให้ตรงกับ limit · `#main` เป็น `100vh` เพื่อดันส่วนท้ายพ้นจอตั้งแต่เฟรมแรก |

**สิ่งที่หน้าเว็บต้องบอกให้ชัด (ทำแล้ว)**

- ป้าย **"ยังไม่ล็อกเวลา" / "ล็อกเวลาให้แล้ว"** บนการ์ดคิวทุกใบ
- กล่องยืนยันการจองบอกกติกาสองข้อ **ก่อน** กด ไม่ใช่ไปรู้ทีหลัง (`.policy-box`)
- กล่องยืนยันการยกเลิกบอกยอดที่จะเสียเป็นตัวเลข
- หน้าจัดการร้านเห็นว่าคิวไหนยังไม่ล็อก และได้ค่ามัดจำจากคิวที่ลูกค้ายกเลิกเท่าไหร่

### รอบตรวจหลังเพิ่ม 4 หมวดกลุ่มผู้ชาย 22 ส.ค. 2026 (รอบที่ 5)

**ที่มาของบั๊กรอบนี้ทั้งหมดเป็นเรื่องเดียวกัน** — โค้ดเดิมเขียนตอนที่โหมด `instant`
มีผู้ใช้อยู่หมวดเดียวคือ "ส่งของด่วน" จึงฝังคำว่าส่งของไว้ตายตัวหลายจุด
พอ "ตัดผมนอกสถานที่" มาใช้โหมดเดียวกัน ป้ายกำกับจึงผิดทันที

| # | อาการ | สาเหตุ | แก้อย่างไร |
|---|---|---|---|
| 52 | หน้าเรียกช่างตัดผมถามว่า **"รับของที่"** และ **"ของที่ส่ง"** ทั้งที่ไม่มีของให้รับ · ปุ่มบอกว่า "จ่ายงานให้พนักงานส่ง" ทั้งที่เป็นช่างตัดผม | `shop.html` เขียนหน้าจอ instant ไว้สำหรับส่งของอย่างเดียว | เพิ่มตัวแปร `IS_COURIER` ดูจาก `category_slug === "delivery"` · ส่งของแสดง 2 ช่องที่อยู่ · **บริการถึงที่แสดงช่องเดียว** เพราะคนออกเดินทางจากร้านอยู่แล้ว · คำเรียกใช้ `RES` แทนคำตายตัว |
| 53 | บริการถึงที่ **ส่งข้อมูลไม่ครบ** เพราะไม่มีช่องต้นทางให้กรอก | เซิร์ฟเวอร์บังคับ `pickup_address` ยาวอย่างน้อย 5 ตัวอักษร | เติมที่อยู่ร้านให้อัตโนมัติเมื่อไม่มีช่องต้นทาง (`shopData.address` + `district`) |
| 54 | การ์ดคิวในหน้าการจองขึ้น **"รับที่ / ส่งที่"** กับงานตัดผม | `bookings.html` ใช้เงื่อนไขเดียวกันหมด | เพิ่ม `isCourier(shopId)` · ส่งของใช้ "รับที่/ส่งที่" · บริการถึงที่ใช้ **"ออกจาก/ไปที่"** · เปลี่ยนชื่อตัวแปร `isDelivery` เป็น `isInstant` ให้ตรงความหมาย |
| 55 | ปุ่มเลือกบริการในหน้าเรียกใช้ทันที **เรียงซ้อนกันเต็มความกว้าง** ไม่เป็นตาราง | ใช้คลาส `.svc-list` ซึ่ง **ไม่เคยมีนิยามใน `app.css` เลย** — พิมพ์ผิดจาก `.svc-grid` ตั้งแต่แรก ไม่มีใครเห็นเพราะหมวดส่งของมีบริการแค่ 3 อันเรียงกันแล้วยังดูโอเค | เปลี่ยนเป็น `.svc-grid` ที่มีนิยามจริง |

**ตรวจแล้วไม่พบปัญหา**

- ฝั่งเซิร์ฟเวอร์ **ไม่มีการฝังชื่อหมวดไว้ในตรรกะเลยสักจุด** (ค้น `"delivery"` `"football"` ฯลฯ ใน `routers/` `models.py` `schemas.py` แล้วว่าง)
- `manage.html` และสลิปพิมพ์ใช้ `RES_RAW()` อยู่แล้ว รองรับคำใหม่ทั้ง แพทย์ · ช่องบริการ · ห้อง
- ทดสอบตรรกะช่วงเวลากับกรณีสุดขั้ว 8 แบบ — งานยาวเกินเวลาเปิดคืน 0 ช่อง ไม่ crash · งานยาว 24 ชม. พอดีในร้านเปิด 24 ชม. คืน 1 ช่องถูกต้อง
- ไม่มีคลาส CSS ที่ใช้แล้วไม่มีนิยามเหลืออีก

> **กฎที่ตั้งจากรอบนี้:** เวลาจะเช็กว่าร้านเป็นประเภทไหน **ให้ดูที่ `category_slug` ไม่ใช่ `resource_label`**
> เพราะคำเรียกทรัพยากรแก้ได้จากฐานข้อมูล แต่ slug เป็นตัวระบุที่โค้ดยึดได้

---

## 6. งานที่เหลือ — ทำต่อจากตรงนี้

### 6.1 ยืนยันแล้วว่าใช้งานได้ (19 ส.ค. 2026)

- บั๊กความจุ #4 — คลินิกวันที่หมอหยุด รับได้ **1 คิว** ตามจำนวนคนที่เข้างานจริง ✓
- บั๊กลบร้าน #11 — ลบร้านที่มีคิวผูกอยู่ 9 รายการได้แล้ว ✓
- โหมดคอนเทนเนอร์เดียว (`SERVE_WEB=true`) — log ขึ้น `เสิร์ฟหน้าเว็บจาก /app/web` ✓
- เปลี่ยนแบรนด์เป็น Bookvice ครบ 52 จุด · ฟอนต์ FC Mittraphap · โลโก้ใหม่ · favicon ใหม่ ✓

### 6.2 ยังไม่ได้ทำ

| เรื่อง | รายละเอียด |
|---|---|
| **รูปภาพจริงของสนาม** | ภาพจากเว็บอ้างอิงมีลิขสิทธิ์ **คัดลอกมาไม่ได้** ทางออก: หาภาพสัญญาอนุญาตเปิด (Unsplash/Pexels) แล้วอัปผ่านแท็บ "รูปภาพ" ในหน้าจัดการร้าน — ระบบอัปโหลดพร้อมใช้แล้ว |
| **แปลงฟอนต์เป็น woff2** | ตอนนี้เป็น `.ttf` 104 KB · แปลงแล้วเหลือราว 35 KB โหลดเร็วขึ้นชัดเจน |
| ยังไม่มี unit test แบบ pytest | ตอนนี้ทดสอบด้วยสคริปต์ยิง API จริง + เปิดหน้าจริงด้วยเบราว์เซอร์จำลอง (ดู `TEST_CHECKLIST.md` หัวข้อ 5) |
| เอกสารส่งอาจารย์ | ผู้ใช้บอกว่า "ยังไม่ต้อง เน้นโค้ดก่อน" |

### 6.3 ฟีเจอร์ที่เคยเสนอแล้วผู้ใช้ยังไม่เลือก

หน้าโปรไฟล์รวมศูนย์ (เคยปฏิเสธ 1 ครั้ง) · แชทสอบถามก่อนจอง · ระบบชำระเงินจริง

---

## 7. ข้อมูลตัวอย่างในระบบ

**38 ร้าน** · 12 หมวด

| หมวด | จำนวน | ที่มาของข้อมูล |
|---|---|---|
| ความงาม 5 หมวด | 11 ร้าน | **แต่งขึ้น** พร้อมรีวิวสมมติ |
| สนามฟุตบอล | 9 แห่ง | **ชื่อสมมติ** แต่พิกัด/ราคา/เวลาทำการอ้างอิงสถานที่จริง |
| สนามแบดมินตัน | 5 แห่ง | เช่นเดียวกัน |
| ส่งของด่วน | 2 ศูนย์ | Bookvice Express พระราม 9 / อารีย์ |
| **คลินิกสุขภาพชาย** | **3 แห่ง** | ชื่อสมมติ · ไม่มีรีวิว |
| **ตัดผมนอกสถานที่** | **2 ศูนย์** | ชื่อสมมติ · โหมด `instant` |
| **ดูแลรถยนต์** | **3 แห่ง** | ชื่อสมมติ · งานยาวสุด 480 นาที |
| **ห้องคาราโอเกะ** | **3 แห่ง** | ชื่อสมมติ · เปิดคร่อมเที่ยงคืนทั้งสามแห่ง |

### 4 หมวดกลุ่มผู้ชาย (เพิ่ม 22 ส.ค. 2026) — เพิ่มมาเพื่อพิสูจน์อะไร

ไม่ได้เลือกจาก "ตลาดใหญ่" แต่เลือกจาก **"หมวดนี้ทดสอบสิ่งที่หมวดเดิมทดสอบไม่ได้"**

| หมวด | เวลาทำการ | สิ่งที่ทดสอบ |
|---|---|---|
| คลินิกสุขภาพชาย | ปกติ | มุมความเป็นส่วนตัว — `_short_name()` + จองแบบ guest |
| ตัดผมนอกสถานที่ | 08:00–00:00 | **หมวดที่สองที่ใช้ `booking_mode="instant"`** ต่อจากส่งของด่วน |
| ดูแลรถยนต์ | 08:00–18:00 | **การจองยาว 4–8 ชม.** ชนกับเวลาปิดร้าน (เคลือบแก้ว 480 นาที เหลือ 5 ช่อง) |
| ห้องคาราโอเกะ | 14:00–02:00 | **คร่อมเที่ยงคืน** — จอง 3 ชม. หลังเที่ยงคืนไม่ได้เพราะช่วงยาวแค่ 120 นาที |

**ตัวเลขที่คำนวณไว้แล้ว ใช้อ้างอิงตอนนำเสนอได้**

| ร้าน | บริการ | ช่องที่จองได้ |
|---|---|---|
| ดูแลรถยนต์ (08:00–18:00) | ล้างรถ 90 นาที | 18 ช่อง · 08:00–16:30 |
| | ติดฟิล์ม 240 นาที | 13 ช่อง · 08:00–14:00 |
| | **เคลือบแก้ว 480 นาที** | **5 ช่อง · 08:00–10:00** |
| คาราโอเกะ (14:00–02:00) | จอง 1 ชม. | 22 ช่อง (หลังเที่ยงคืน 3: 00:00 · 00:30 · 01:00) |
| | จอง 2 ชม. | 18 ช่อง (หลังเที่ยงคืน 1: 00:00) |
| | **จอง 3 ชม.** | **15 ช่อง (หลังเที่ยงคืน 0)** |

### ⚠️ กับดักที่ทำให้ข้อมูลซ้ำ — เคยพลาดมาแล้ว 2 ครั้ง

`seed_venues()` ตรวจข้อมูลซ้ำ **จากชื่อร้าน** (`WHERE Shop.name == name`)

**ถ้าแก้ชื่อใน `seed_venues.py` แล้วรีสตาร์ต ระบบจะมองว่าเป็นร้านใหม่แล้วสร้างซ้ำทั้งชุด**

- ครั้งที่ 1: เปลี่ยนชื่อสนามเป็นชื่อสมมติ → ได้ 41 ร้านแทน 27
- ครั้งที่ 2: เปลี่ยน GlowGo Express → Bookvice Express → ต้องรีบเปลี่ยนชื่อในฐานข้อมูลตามทันก่อนรีสตาร์ต

**ถ้าจะเปลี่ยนชื่อร้านในไฟล์ seed ต้องเปลี่ยนในฐานข้อมูลด้วยเสมอ**
เปลี่ยนผ่าน `PUT /api/shops/{id}` ด้วยบัญชี admin หรือลบชุดเดิมทิ้ง

### กฎที่ยึดไว้กับข้อมูลสถานที่ — อย่าเปลี่ยน

1. **ชื่อร้านทั้งหมดเป็นชื่อสมมติ** — เว็บนี้เปิดสาธารณะได้ ถ้าใช้ชื่อธุรกิจจริงคู่กับปุ่ม "จองคิว" ที่กดได้ จะกลายเป็นการแสดงว่าร้านเหล่านั้นรับจองผ่านเรา ทั้งที่เขาไม่เคยตกลงด้วย
2. **ไม่ใส่เบอร์โทรจริง** — ถ้ามีคนโทรไปตามคิวที่ไม่มีอยู่จริง ร้านเดือดร้อน
3. **ไม่แต่งรีวิวให้สนาม** — สนามทุกแห่งเริ่มที่ 0 รีวิว
4. ใช้ได้แค่ **พิกัด · เขต · ช่วงราคาที่ประกาศ · เวลาทำการ** ซึ่งเป็นข้อเท็จจริงสาธารณะ
5. footer มีข้อความกำกับว่าข้อมูลเป็นตัวอย่างและการชำระเงินเป็นการจำลอง


## 8. สิ่งที่ผู้ใช้ย้ำหลายครั้ง — ต้องเคารพ

| สิ่งที่พูด | หมายความว่า |
|---|---|
| "ไม่เอาให้ดูเหมือน AI" | ห้ามใส่กล่องตัวเลขอวดขนาด · ห้ามส่วน "3 ขั้นตอน" พร้อมเลขจาง ๆ · ห้ามป้ายตัวเล็กพิมพ์ใหญ่เหนือหัวข้อ · ห้ามการ์ดสามใบเรียงกันแบบเทมเพลต |
| "ขอมืออาชีพ ให้ดูน่าเชื่อถือ" | แนวสงบ มั่นใจ แบบเว็บจองโรงแรม **ไม่เอาแนวตลาดนัดดีลลดราคา** (ผู้ใช้บอกตรง ๆ ว่า GoWabi ดูกาก) |
| "อย่าทำให้ใช้งานยาก" | ทุกฟีเจอร์ที่เพิ่มต้องไม่ทำให้ขั้นตอนเดิมยาวขึ้น |
| "ขออนิเมชันนิดเดียว" | การเคลื่อนไหวต้องเบา ใช้จังหวะชุดเดียวกันทั้งเว็บ (`--ease` / `--t` ใน `app.css`) |
| "เช็คบัคให้ละเอียด" | ผู้ใช้คาดหวังการทดสอบจริงผ่าน API ไม่ใช่แค่อ่านโค้ด |

### สิ่งที่ตัดออกไปแล้วเพราะ "ทำระบบไม่ได้จริง" — อย่าเอากลับมา

ส่วนลด 20% · สะสมแต้ม · สมาชิก GlowGo+ · ข่าวประชาสัมพันธ์ที่แต่งขึ้น ·
เบอร์ Call Center 1330 (เป็นเบอร์ สปสช. จริง) · ลิงก์ Facebook/IG/LINE ที่กดไม่ได้

> **หลักการ:** อะไรที่หน้าเว็บอ้าง ระบบต้องทำได้จริง
> นี่คือสิ่งที่ทำลายความน่าเชื่อถือหนักกว่าเรื่องดีไซน์

---

### ไฟล์เอกสารในโปรเจกต์

| ไฟล์ | เนื้อหา |
|---|---|
| `PROJECT_CONTEXT.md` | ไฟล์นี้ — สถานะปัจจุบันทั้งหมด |
| `README.md` | อ่านก่อนใช้งานในเครื่อง · รายการ API ครบ 56 เส้น |
| `DEPLOY_RENDER.md` | คู่มือนำขึ้น Render + Neon ทีละขั้น |
| `DEMO_GUIDE.md` | คู่มือซ้อมนำเสนอ · แผนสำรองถ้า Docker ไม่ขึ้น |
| `TEST_CHECKLIST.md` | รายการทดสอบทุกรอบ พร้อมผลที่รันไปแล้ว |

---

## 8.5 การนำขึ้นเว็บสาธารณะ — ใช้ Render

**เว็บจริงอยู่ที่ https://bookvice.onrender.com** · ฐานข้อมูลอยู่บน Neon
คู่มือทีละขั้นอยู่ใน `DEPLOY_RENDER.md`

> **เคยเตรียมขึ้น Hugging Face ไว้ แต่เลิกใช้แล้วเมื่อ 20 ส.ค. 2026**
> เพราะ Docker Space ไม่ฟรีสำหรับงานแบบนี้อีกต่อไป
> เอกสาร `DEPLOY_HUGGINGFACE.md` และ `README_HF.md` ถูกลบทิ้งเมื่อ 22 ส.ค. 2026

### ⚠️ ชื่อไฟล์ที่ทำให้เข้าใจผิดได้ง่ายที่สุดในโปรเจกต์

**`Dockerfile.hf` คือไฟล์ที่ Render ใช้ build จริง — ห้ามลบ**

ชื่อยังติด `.hf` มาจากตอนที่ตั้งใจจะขึ้น Hugging Face แต่ตอนเปลี่ยนมาใช้ Render
ก็ใช้ไฟล์เดิมได้เลยเพราะโครงเหมือนกัน (คอนเทนเนอร์เดียว อ่านพอร์ตจากตัวแปรแวดล้อม)

| ไฟล์ | ใครใช้ |
|---|---|
| `Dockerfile` | `docker-compose.yml` ตอนรันในเครื่อง |
| **`Dockerfile.hf`** | **Render** — ตั้งไว้ที่ Settings → Dockerfile Path = `./Dockerfile.hf` |

ถ้าจะเปลี่ยนชื่อเป็น `Dockerfile.render` ให้ตรงความจริง
**ต้องไปแก้ค่าใน Render Settings ด้วยในคราวเดียวกัน ไม่งั้น deploy ครั้งถัดไปพัง**

**สวิตช์ 2 ตัวใน `config.py`** — ปิดอยู่โดยค่าเริ่มต้น จึงไม่กระทบ compose

| ตัวแปร | ค่าเริ่มต้น | ตั้งเป็นอะไรบน Render |
|---|---|---|
| `SERVE_WEB` | `false` | `true` — ให้ FastAPI เสิร์ฟหน้าเว็บเอง |
| `UPLOAD_DIR` | `/app/uploads` | `/tmp/uploads` — เขียน `/app` ไม่ได้ |

`web/js/api.js` ตรวจพอร์ตเอง — **พอร์ต 3000 เรียก `:8000` · พอร์ตอื่นเรียก same-origin**

### สิ่งที่ตรวจพบบน Render เมื่อ 22 ส.ค. 2026 — ยังไม่ได้แก้

| เรื่อง | อาการ |
|---|---|
| **Auto-Deploy ไม่ทำงาน** | ตั้งเป็น "On Commit" แล้วแต่ webhook ไม่เคยยิง · Events มีแค่ deploy แรกวันที่ 21 ส.ค. · **ตอนนี้ต้องกด Manual Deploy → Deploy latest commit เองทุกครั้งที่ push** |
| **ไม่มี `ADMIN_PASSWORD`** | Environment มี 5 ตัว (`DATABASE_URL` · `JWT_SECRET` · `SEED_ON_START` · `SERVE_WEB` · `UPLOAD_DIR`) ขาดตัวนี้ ระบบจึงสุ่มรหัสแล้วพิมพ์ลง log ครั้งเดียว **แปลว่าเข้าบัญชี admin บนเว็บจริงไม่ได้** |
| เซิร์ฟเวอร์หลับ | แผนฟรีหลับเมื่อไม่มีคนใช้ 15 นาที ปลุกใหม่ราว 50 วินาที · **เปิดลิงก์ทิ้งไว้ก่อนนำเสนอ** |

---

## 9. วิธีทดสอบที่ใช้อยู่

ไม่มี test suite — ทดสอบด้วยการยิง API จริงจาก console ของเบราว์เซอร์

```js
const B = "http://localhost:8000";
const tok = async u => (await (await fetch(B+"/api/auth/login", {
  method:"POST", headers:{"Content-Type":"application/json"},
  body: JSON.stringify({username:u, password:"Password123"})
})).json()).access_token;

const T = { cust: await tok("mind"), owner: await tok("spaowner"), admin: await tok("admin") };
const J = (t,m,b) => ({
  method: m||"GET",
  headers: { Authorization:"Bearer "+t, "Content-Type":"application/json" },
  ...(b ? {body: JSON.stringify(b)} : {})
});
```

**อย่าลืมลบคิวทดสอบทิ้งหลังทดสอบเสร็จ** — ไม่งั้นจะไปโผล่ในรายงานยอดขายของเจ้าของร้าน

### ชุดทดสอบที่ควรรันซ้ำหลังแก้ระบบจอง

1. ช่างมีวันหยุด → วันนั้นต้องปิดทุกช่อง
2. ช่างมีเวลาเข้า-ออก → ช่องเวลาต้องอยู่ในกรอบของช่าง ไม่ใช่ของร้าน
3. **ช่องที่ตารางบอกว่าง ต้องจองได้จริง** (ความสอดคล้องระหว่าง availability กับ create_booking)
4. จองกับช่าง A แล้ว ช่าง B ต้องยังว่างเวลาเดียวกัน
5. สนาม 24 ชม. ต้องมีช่องเวลา (ไม่ใช่ 0)
6. สนามคร่อมเที่ยงคืนต้องได้สองช่วง
7. คิว 23:00–00:00 ต้องกันคิว 22:30 ที่คาบเกี่ยว
8. ไม่เลือกช่าง → รับได้เท่าจำนวนคนที่เข้างานวันนั้น **(ข้อนี้เคยพลาด)**
9. ส่งของด่วน: คิดเงินถูก · ยืนยันอัตโนมัติ · จองผ่านปฏิทินต้องโดน 400

---

## 10. ข้อจำกัดของสภาพแวดล้อมที่ทำงาน

- **พิมพ์ลงเทอร์มินัลไม่ได้** — ต้องส่งคำสั่งให้ผู้ใช้รันเอง (ใส่คลิปบอร์ดให้)
- ถ้า WebFetch/WebSearch บอกว่าเข้าโดเมนไม่ได้ **ห้ามเลี่ยงไปใช้ curl/python**
- นามสกุลผู้รีวิวถูกย่อเหลือตัวอักษรเดียวเสมอ (`_short_name` ใน `bookings.py`) — **ห้ามเปิดเผยนามสกุลเต็ม**

---

## 11. สรุปสิ่งที่ค้างอยู่ ณ ตอนนี้

เรียงตามความสำคัญ — ทำจากบนลงล่างได้เลย

### ทำเสร็จแล้วเมื่อ 19 ส.ค. 2026

- `manage.html` และ `bookings.html` ใช้ `resource_label` ครบแล้ว เจ้าของสนามเห็นคำว่า "คอร์ท" / "สนาม" ✓
- มี `DELETE /api/reviews/{id}` แล้ว — เจ้าของรีวิวและ admin ลบได้ เจ้าของร้านลบไม่ได้ ✓
- เอกสาร `README.md` เขียนใหม่ให้ตรงของจริง (56 endpoint · 12 ตาราง · 8 หมวด) ✓
- เปลี่ยนแบรนด์เป็น Bookvice ครบทุกจุดรวมฐานข้อมูลและ container ✓
- ปรับปรุงการตอบสนองตอนคลิกทั้งเว็บ (ดูหัวข้อ 13) ✓
- **ตรวจบั๊กรอบละเอียด (รอบที่ 2)** — แก้ 21 จุด ฝั่งเซิร์ฟเวอร์ 13 · หน้าเว็บ 8 (ดูตารางบั๊ก #12–#32) ✓
- **แก้อาการ "ยังดูไม่สมูท"** — จองพื้นที่ล่วงหน้าใน CSS + โครงร่างรอโหลดของ `shop.html` CLS ลดจาก 0.12/0.29/0.70 เหลือ 0.014/0.030/0 ✓
- ชุดทดสอบอัตโนมัติ: ไวยากรณ์ JS 12 ไฟล์ · ตรรกะการจอง 13 ข้อ · เปิดหน้าจริงด้วยเบราว์เซอร์จำลอง 10 หน้า (รวมหน้าที่ต้องล็อกอิน กดครบทุกแท็บ) ✓

### ยังเหลือ

**1. รูปภาพจริงของร้านและสนาม** — ตอนนี้การ์ดร้านทุกใบเป็นภาพไล่สีที่วาดด้วยโค้ด
ซึ่งทำให้หน้าแรกดูเหมือนยังไม่ได้ใส่ของ **นี่คือสิ่งที่ทำให้เว็บดูไม่เสร็จมากที่สุดตอนนี้**
ทางออก: หาภาพสัญญาอนุญาตเปิด (Unsplash/Pexels) แล้วอัปผ่านแท็บ "รูปภาพ" ในหน้าจัดการร้าน
ระบบอัปโหลดพร้อมใช้แล้ว

**2. ยังไม่มี unit test** — ทดสอบด้วยการยิง API จริงผ่านเบราว์เซอร์เท่านั้น (ดู `TEST_CHECKLIST.md`)

**3. แปลงฟอนต์เป็น woff2** — ตอนนี้เป็น `.ttf` 104 KB แปลงแล้วเหลือราว 35 KB

### พร้อมทำได้ทันทีถ้าผู้ใช้สั่ง

**4. เปิด Auto-Deploy ของ Render ให้ทำงาน** — ตอนนี้ตั้งเป็น "On Commit" แล้วแต่ webhook ไม่ยิง
ทุกครั้งที่ push ต้องเข้าไปกด Manual Deploy เอง · แก้ถาวรได้ที่ Settings → Source → Edit แล้วเชื่อม GitHub ใหม่

**5. เพิ่ม `ADMIN_PASSWORD` ใน Render Environment** — ตอนนี้ไม่มี ทำให้เข้าบัญชี admin บนเว็บจริงไม่ได้

---

## 12. เช็กลิสต์ก่อนส่งงาน

- [ ] `docker compose up -d --build` แล้วเปิด `localhost:3000` ได้
- [ ] `localhost:8000/docs` แสดง endpoint ครบ 56 เส้น (นับ path+method ไม่ใช่จำนวน path)
- [ ] จองคิวได้จริงตั้งแต่ต้นจนจบ (เลือกบริการ → เลือกวัน → เลือกเวลา → ยืนยัน)
- [ ] เจ้าของร้านเห็นคิวที่ลูกค้าจองเข้ามาและกดยืนยันได้
- [ ] ชำระมัดจำแล้วได้ใบเสร็จ
- [ ] แผนที่ในหน้าร้านแสดงหมุดถูกตำแหน่ง
- [ ] ปุ่ม "ใกล้ฉัน" ในหน้าค้นหาเรียงร้านตามระยะทาง
- [ ] เรียกใช้บริการส่งของด่วนได้โดยไม่ต้องเลือกวันเวลา
- [ ] ไม่มีคำว่า GlowGo หลงเหลือที่ไหน (ยกเว้น 3 จุดที่ตั้งใจเก็บไว้ ดูหัวข้อ 1)
- [ ] ฟอนต์ FC Mittraphap แสดงผลถูกต้อง (หัวข้อเป็นทรงเรขาคณิตปลายเปิด)


---

## 12.5 ตัด Tailwind CDN ออก (19 ส.ค. 2026)

เดิมทุกหน้าโหลด `https://cdn.tailwindcss.com` ซึ่งเป็นสคริปต์ที่ต้องดาวน์โหลดจากอินเทอร์เน็ต
แล้ว **คอมไพล์ CSS ในเบราว์เซอร์** ตอนเปิดหน้า ผลคือผู้ใช้เห็นหน้าเว็บแบบไม่มีสไตล์แวบหนึ่ง
ก่อนจะจัดใหม่ทั้งหน้า และถ้าเน็ตหลุดตอนนำเสนอ หน้าเว็บจะเละทั้งหน้า

นับแล้วโปรเจกต์นี้ใช้คลาสของ Tailwind จริง ๆ แค่ **123 ตัว** (จากคลาสทั้งหมด 386 ตัว
อีก 274 ตัวนิยามเองใน `app.css` อยู่แล้ว) และไม่มี `hover:` / `focus:` เลยสักตัว
จึงเขียนลง `app.css` ตรง ๆ แล้วตัด CDN ทิ้งได้

**สิ่งที่เพิ่มเข้าไปใน `app.css` มีสองก้อน อย่าสลับลำดับ**

| ก้อน | อยู่ตรงไหน | ทำไมต้องอยู่ตรงนั้น |
|---|---|---|
| รีเซ็ตพื้นฐาน (แทน Preflight) | **บนสุด** ก่อน `:root` | ต้องมาก่อนสไตล์คอมโพเนนต์ ไม่งั้นจะไปทับสไตล์ที่เขียนไว้ |
| ยูทิลิตี้ (`.mt-5` `.flex` …) | **ล่างสุด** ท้ายไฟล์ | ต้องชนะสไตล์คอมโพเนนต์ เหมือนที่ Tailwind เคยทำ |

> **รีเซ็ตสำคัญมาก อย่าลบ** — โค้ดทั้งโปรเจกต์เขียนโดยตั้งอยู่บนสมมติฐานว่ามี Preflight
> ถ้าตัด CDN ทิ้งเฉย ๆ โดยไม่ใส่รีเซ็ตกลับมา `<p>` จะมีระยะห่างโผล่มาเอง
> `<ul>` จะมีจุดนำหน้า และปุ่มจะกลายเป็นปุ่มเทาของ Windows

**ถ้าจะเพิ่มคลาสยูทิลิตี้ตัวใหม่ในอนาคต ต้องไปเพิ่มในหัวข้อท้ายไฟล์ `app.css` เอง**
เขียน `class="mt-9"` เฉย ๆ จะไม่มีผลอะไรเลย เพราะไม่มี Tailwind คอยสร้างให้แล้ว
สเกลระยะห่างอิงของ Tailwind คือ `1 = 0.25rem` (เช่น `mt-5` = `1.25rem`)

ผลที่วัดได้: CLS ของทุกหน้าลดลงอีก เช่น `shops.html` จาก 0.0346 เหลือ 0.0013
และ `promotions.html` จาก 0.0596 เหลือ 0

---

## 13. ชั้นการตอบสนองตอนคลิก (เพิ่มเมื่อ 19 ส.ค. 2026)

เดิมแต่ละหน้าเขียนโค้ดจัดการปุ่มและกล่องยืนยันของตัวเอง ทำให้หน้าตาไม่ตรงกัน
ตอนนี้รวมไว้ที่ `web/js/ui.js` ชุดเดียว **เวลาจะเพิ่มปุ่มใหม่ให้เรียกใช้ตัวเหล่านี้ อย่าเขียนใหม่**

| ฟังก์ชัน | ใช้ทำอะไร |
|---|---|
| `btnBusy(btn, on, label)` | ใส่สปินเนอร์ในปุ่มและตรึงความกว้างเดิมไว้ ปุ่มจึงไม่หดตอนเปลี่ยนข้อความ |
| `btnOk(btn, label, ms)` | โชว์เครื่องหมายถูกสั้น ๆ แล้วคืนสภาพ ใช้กับงานที่จบในที่ |
| `withBusy(btn, label, fn)` | ครอบงานที่ต้อง await ให้คืนปุ่มเสมอแม้ error |
| `confirmDialog({...})` | แทน `confirm()` — คืน Promise<boolean> · `tone: "danger"` ทำให้ปุ่มยืนยันแดงทึบและโฟกัสปุ่มยกเลิกไว้ก่อน |
| `promptDialog({...})` | แทน `prompt()` — คืน Promise<string\|null> มีตัวนับตัวอักษร กด Ctrl+Enter ส่งได้ |

**กฎที่ตั้งไว้:**

- ปุ่มที่ยิง API ทุกปุ่มต้องผ่าน `btnBusy` — ห้ามเขียน `btn.textContent = "กำลัง..."` เองอีก
- ห้ามใช้ `confirm()` / `prompt()` / `alert()` ของเบราว์เซอร์
- เปิด/ปิดกล่องซ้อนต้องใช้ `openModal()` / `closeModalEl()` เท่านั้น
  สองตัวนี้ขังโฟกัสไว้ในกล่องและคืนโฟกัสให้ปุ่มที่กดเปิด ถ้าไปเติม `is-open` เองจะเสียทั้งสองอย่าง
- ทุกหน้าต้องมี handler ปิดกล่องด้วย Esc **และต้องเช็ก `is-open` ก่อนเสมอ**
  ไม่งั้นการกด Esc ตอนไม่มีกล่องเปิดจะไปดึงโฟกัสกลับไปที่ปุ่มเก่า
- แอนิเมชันใช้ `--ease` / `--t` ใน `app.css` ชุดเดิมเท่านั้น ห้ามตั้งจังหวะใหม่

### เลข `?v=` ท้ายไฟล์ css/js

ทุกหน้าอ้าง `css/app.css?v=...` และ `js/ui.js?v=...` ด้วยเลขเดียวกัน
**แก้ไฟล์ใน `web/css` หรือ `web/js` แล้วต้องขยับเลขนี้ทุกหน้าด้วย**
ไม่งั้นเบราว์เซอร์ของคนที่เคยเข้าแล้วจะใช้ไฟล์เก่าต่อไปจนกว่าจะกด Ctrl+Shift+R
